import React from 'react';
import { shallow } from 'enzyme';
import GridHeader from './GridHeader';

describe('Grid Header', () => {
  test('It should render', () => {
    shallow(<GridHeader>{}</GridHeader>);
  });
});
